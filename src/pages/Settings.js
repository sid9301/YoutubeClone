import React from 'react';

const Settings = () => (
  <div className="p-6">
    <h1 className="text-2xl font-semibold">Settings</h1>
    <p className="mt-2 text-gray-600">App settings and preferences.</p>
  </div>
);

export default Settings;
